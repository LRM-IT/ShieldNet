# GuildConsole

GuildConsole is a Discord server management platform covering members, roles, moderation, automation, plugins, and security. The existing `shieldnet` repository, service names, environment variables, database, and API identifiers remain unchanged during the migration.

Docker deployment instructions: [docker/README.md](docker/README.md).

