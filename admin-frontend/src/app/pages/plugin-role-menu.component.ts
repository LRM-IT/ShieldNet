import {Component,OnInit,inject,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {HttpClient} from '@angular/common/http';
import {ActivatedRoute} from '@angular/router';
import {firstValueFrom} from 'rxjs';
import {ShellComponent} from '../shared/shell.component';
import {DiscordChannelPickerComponent} from '../shared/discord-channel-picker.component';
import {GuildPluginService} from '../core/guild-plugin.service';

interface Role{id:string;name:string;managed:boolean;assignable:boolean}
interface Option{label:string;role_id:string;emoji:string;description:string}
interface Panel{id:string;name:string;title:string;description:string;enabled:boolean;channel_id:string|null;required_role_id:string|null;mode:'buttons'|'select';max_roles:number;options:Option[];message_id?:string|null;expanded?:boolean}
interface Settings{installed:boolean;enabled:boolean;panels:Panel[]}

@Component({standalone:true,imports:[FormsModule,ShellComponent,DiscordChannelPickerComponent],template:`
<sn-shell title="Role Menu"><main class="page">
  <header><span>GUILD PLUGIN</span><h2>Role Menu</h2><p>Members choose their own Discord roles using buttons or a select menu.</p></header>
  @if(error()){<div class="notice error">{{error()}}</div>} @if(success()){<div class="notice success">{{success()}}</div>}
  @if(!settings.installed){<section class="card"><p>Install the plugin to create role panels.</p><button (click)="install()" [disabled]="busy()">Install plugin</button></section>}
  @else {
    <section class="card heading"><div><h3>Role panels</h3><p>Create independent panels for game, notification, region or access roles.</p></div><div class="actions"><button (click)="addPanel()">Add panel</button></div></section>
    @for(panel of settings.panels;track panel.id;let pi=$index){
      <article class="card panel">
        <button class="summary" (click)="panel.expanded=!panel.expanded"><span><strong>{{panel.name||'New panel'}}</strong><small>{{panel.options.length}} roles · {{panel.mode}} · {{panel.enabled?'Enabled':'Disabled'}}</small></span><b [class.open]="panel.expanded">⌄</b></button>
        @if(panel.expanded){<div class="body">
          <div class="form-grid"><label><span>Panel name</span><input [(ngModel)]="panel.name" maxlength="80"></label><label><span>Discord title</span><input [(ngModel)]="panel.title" maxlength="100"></label></div>
          <label class="field"><span>Description</span><textarea [(ngModel)]="panel.description" rows="3" maxlength="1000"></textarea></label>
          <div class="form-grid"><label><span>Channel</span><sn-discord-channel-picker [guildId]="guildId" [value]="panel.channel_id" (valueChange)="panel.channel_id=$event" /></label><label><span>Required role</span><select [(ngModel)]="panel.required_role_id"><option [ngValue]="null">Everyone</option>@for(role of roles();track role.id){<option [value]="role.id">{{role.name}}</option>}</select></label></div>
          <div class="form-grid settings-grid"><label><span>Display</span><select [(ngModel)]="panel.mode"><option value="buttons">Buttons</option><option value="select">Select menu</option></select></label><label><span>Maximum selected roles</span><input type="number" min="0" max="25" [(ngModel)]="panel.max_roles"><small>0 means unlimited.</small></label><div class="field toggle-field"><span>Panel status</span><label class="check"><input type="checkbox" [(ngModel)]="panel.enabled"><span>Panel enabled</span></label></div></div>
          <div class="options"><div class="options-heading"><div><h4>Roles</h4><small>Configure the label, emoji, Discord role and description.</small></div><button class="secondary" (click)="addOption(panel)">Add role</button></div>
            @for(option of panel.options;track $index;let oi=$index){<div class="option"><label>Label<input [(ngModel)]="option.label" maxlength="80"></label><label>Emoji<input [(ngModel)]="option.emoji" maxlength="40" placeholder="🎮"></label><label>Discord role<select [(ngModel)]="option.role_id"><option value="">Select role</option>@for(role of assignableRoles();track role.id){<option [value]="role.id">{{role.name}}</option>}</select></label><label>Description<input [(ngModel)]="option.description" maxlength="100"></label><button class="danger" (click)="panel.options.splice(oi,1)">Remove</button></div>}
          </div>
          <div class="footer"><button class="danger" (click)="settings.panels.splice(pi,1)">Delete panel</button><span></span><button class="secondary" (click)="save()">Save</button><button (click)="publish(panel)" [disabled]="!settings.enabled||!panel.enabled">Save and publish</button></div>
        </div>}
      </article>
    } @empty {<section class="card empty">No role panels yet.</section>}
    <button class="save" (click)="save()" [disabled]="busy()">Save all settings</button>
  }
</main></sn-shell>`,styles:[`
.page{max-width:1150px;margin:auto;display:grid;gap:1rem;padding-bottom:3rem}header span{color:var(--primary);font-size:.7rem;font-weight:800;letter-spacing:.12em}h2{margin:.3rem 0;font-size:1.8rem}h3,h4,p{margin:.2rem 0}p,small{color:var(--muted)}.card,.notice{padding:1.2rem;border:1px solid var(--line);border-radius:16px;background:var(--panel)}.heading,.actions,.footer{display:flex;align-items:center;justify-content:space-between;gap:.7rem}.panel{padding:0;overflow:hidden}.summary{width:100%;min-height:0;padding:1rem 1.2rem;display:flex;justify-content:space-between;align-items:center;text-align:left;background:transparent;color:var(--text);border:0}.summary span{display:grid;gap:.25rem}.summary b{font-size:1.5rem;transition:.2s}.summary b.open{transform:rotate(180deg)}.body{display:grid;gap:1.15rem;padding:1.2rem;border-top:1px solid var(--line)}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem;align-items:start}.grid.three{grid-template-columns:repeat(3,minmax(0,1fr))}.options{display:grid;gap:.8rem;padding:1rem;border:1px solid var(--line);border-radius:12px;background:rgba(0,0,0,.05)}.option{display:grid;grid-template-columns:minmax(130px,1fr) 90px minmax(160px,1fr) minmax(160px,1fr) auto;gap:.7rem;align-items:end;padding-top:.8rem;border-top:1px solid var(--line)}label{display:grid;grid-template-rows:auto minmax(48px,auto);gap:.45rem;min-width:0;color:var(--muted);font-size:.78rem;font-weight:650}label small{min-height:1rem;font-size:.68rem;line-height:1.4}.grid.three label:has(small){grid-template-rows:auto minmax(48px,auto) auto}label.check{box-sizing:border-box;display:flex;align-items:center;gap:.65rem;align-self:start;min-height:75px;margin-top:1.45rem;padding:.75rem;border:1px solid var(--line);border-radius:9px;background:var(--panel-2);color:var(--text);cursor:pointer}label.check input{flex:0 0 18px;width:18px;height:18px;margin:0;accent-color:var(--primary)}input:not([type=checkbox]),select,textarea{box-sizing:border-box;width:100%;min-height:48px;padding:.72rem .8rem;border:1px solid var(--line);border-radius:9px;background:var(--panel-2);color:var(--text);font:inherit;outline:none}textarea{min-height:94px;resize:vertical;line-height:1.5}input:not([type=checkbox]):focus,select:focus,textarea:focus{border-color:var(--primary);box-shadow:0 0 0 2px var(--primary-soft)}button{min-height:44px;padding:.72rem 1rem;border:0;border-radius:9px;background:var(--primary);color:#07120f;font-weight:800;cursor:pointer}button.secondary,button.danger{background:transparent;color:var(--text);border:1px solid var(--line)}button.danger{color:#ff9ea3;border-color:rgba(255,90,100,.35)}button:disabled{cursor:not-allowed;opacity:.5}.footer span{flex:1}.save{justify-self:end;min-width:170px}.error{color:#ff9ea3}.success{color:#76e8b8}.empty{text-align:center;color:var(--muted)}@media(max-width:980px){.option{grid-template-columns:repeat(2,minmax(0,1fr))}.option button{grid-column:2;justify-self:end}}@media(max-width:800px){.grid,.grid.three,.option{grid-template-columns:1fr}.heading,.actions,.footer{align-items:stretch;flex-direction:column}.heading button,.actions button,.footer button,.save{width:100%}.footer span{display:none}.option button{grid-column:auto;justify-self:stretch}label.check{margin-top:0}.options{padding:.8rem}}
.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem;align-items:start}.settings-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.form-grid>label,.field,.toggle-field,.option>label{display:grid;grid-template-rows:1.2rem 52px auto;align-content:start;gap:.45rem;min-width:0;color:var(--muted);font-size:.78rem;font-weight:650}.field:has(textarea){grid-template-rows:1.2rem auto}.form-grid>label>span:first-child,.field>span:first-child,.option>label{line-height:1.2}.form-grid input:not([type=checkbox]),.form-grid select,.option input,.option select{height:52px;min-height:52px}.form-grid sn-discord-channel-picker{height:52px}.toggle-field .check{display:flex;align-items:center;align-self:stretch;gap:.65rem;height:52px;min-height:52px;margin:0;padding:.75rem .8rem;border:1px solid var(--line);border-radius:9px;background:var(--panel-2);color:var(--text)}.toggle-field .check span{line-height:1.25}.options{background:var(--panel-2)}.options-heading{display:flex;align-items:center;justify-content:space-between;gap:1rem}.options-heading>div{display:grid;gap:.2rem}.options-heading small{font-size:.7rem}.option{align-items:start}.option>button{height:52px;align-self:start;margin-top:1.65rem;white-space:nowrap}@media(max-width:980px){.settings-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.toggle-field{grid-column:1/-1}.option>button{grid-column:2}}@media(max-width:800px){.form-grid,.settings-grid{grid-template-columns:1fr}.toggle-field{grid-column:auto}.options-heading{align-items:stretch;flex-direction:column}.options-heading button{width:100%}.option>button{grid-column:auto;width:100%;margin-top:0}.form-grid>label,.field,.toggle-field,.option>label{grid-template-rows:auto 52px auto}}
`]})
export class PluginRoleMenuComponent implements OnInit{
 private http=inject(HttpClient);private route=inject(ActivatedRoute);private plugins=inject(GuildPluginService);readonly guildId=this.route.snapshot.paramMap.get('guildId')||'';roles=signal<Role[]>([]);assignableRoles=signal<Role[]>([]);busy=signal(false);error=signal('');success=signal('');settings:Settings={installed:false,enabled:false,panels:[]};private get url(){return `/api/v1/discord/guilds/${this.guildId}/plugins/role-menu`;}
 async ngOnInit(){try{const [settings,structure]=await Promise.all([firstValueFrom(this.http.get<Settings>(`${this.url}/settings`)),firstValueFrom(this.http.get<any>(`/api/v1/discord/guilds/${this.guildId}/structure`))]);this.settings={...settings,panels:(settings.panels||[]).map(x=>({...x,expanded:false}))};const roles=(structure.roles||[]).filter((x:Role)=>!x.managed&&x.name!=='@everyone');this.roles.set(roles);this.assignableRoles.set(roles.filter((x:Role)=>x.assignable));}catch(e:any){this.error.set(e?.error?.detail||'Could not load Role Menu.')}}
 addPanel(){const id=`panel-${Date.now().toString(36)}`;this.settings.panels.push({id,name:'New panel',title:'Choose your roles',description:'',enabled:true,channel_id:null,required_role_id:null,mode:'buttons',max_roles:0,options:[],expanded:true});}
 addOption(panel:Panel){panel.options.push({label:'New role',role_id:'',emoji:'',description:''});}
 payload(){return{panels:this.settings.panels.map(({expanded,message_id,...panel})=>panel)}}
 async install(){this.busy.set(true);try{await this.plugins.install(this.guildId,'role_menu');await this.ngOnInit()}catch(e:any){this.error.set(e?.error?.detail||'Could not install Role Menu.')}finally{this.busy.set(false)}}
 async toggle(){this.busy.set(true);try{if(this.settings.enabled)await this.plugins.disable(this.guildId,'role_menu');else await this.plugins.enable(this.guildId,'role_menu');await this.ngOnInit()}catch(e:any){this.error.set(e?.error?.detail||'Could not change plugin state.')}finally{this.busy.set(false)}}
 async save(){this.busy.set(true);this.error.set('');try{const open=new Set(this.settings.panels.filter(x=>x.expanded).map(x=>x.id));const saved=await firstValueFrom(this.http.put<Settings>(`${this.url}/settings`,this.payload()));this.settings={...saved,panels:saved.panels.map(x=>({...x,expanded:open.has(x.id)}))};this.success.set('Role Menu settings saved.')}catch(e:any){this.error.set(e?.error?.detail||'Could not save settings.')}finally{this.busy.set(false)}}
 async publish(panel:Panel){await this.save();if(this.error())return;this.busy.set(true);try{const result=await firstValueFrom(this.http.post<any>(`${this.url}/publish`,{panel_id:panel.id}));for(let i=0;i<30;i++){await new Promise(r=>setTimeout(r,1000));const job=await firstValueFrom(this.http.get<any>(`${this.url}/jobs/${result.job_id}`));if(job.status==='completed'){this.success.set('Role panel published in Discord.');break}if(job.status==='failed')throw new Error(job.error||'Publication failed')}}catch(e:any){this.error.set(e?.error?.detail||e?.message||'Could not publish panel.')}finally{this.busy.set(false)}}
}
